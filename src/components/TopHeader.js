import React, { Component } from 'react';

export default class TopHeader extends Component {
  constructor(props) {
    super(props);
    this.state = {
      mobileMenu: false,
    }
  }

  render() {
    return (
      <nav>

      </nav>
    )
  }
};
